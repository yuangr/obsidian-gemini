# Daily changelog — July 14, 2026

**Date:** 2026-07-14
**PRs merged:** 5

---

## Summary

An `audit-architecture`-and-`auto-dev`-heavy day: four DRY-consolidation refactors landed back-to-back within minutes of each other late in the day, plus the prior day's automated housekeeping PR.

## What shipped

### [#1211 chore(daily): 2026-07-13 daily update](https://github.com/allenhutchison/obsidian-gemini/pull/1211)

The prior day's automated `daily-update` run: wrote `planning/changelog/2026-07-13.md` (2 PRs merged on 2026-07-13 — the shared system-folder guard extraction and the 2026-07-12 daily update), fixed 9 drift items across `docs/guide/` and `docs/reference/` (several conceptual, not just stale names — e.g. removed false claims about completions respecting the active custom prompt, corrected the context-system `@`-escape mechanism, flagged unsupported Office-document attachments in semantic search, and added the missing Google Maps grounding row to the provider-capabilities table), and a no-op `triage-issues` pass. Housekeeping-only.

### [#1207 refactor(tools): extract shared isFileInAgentScope predicate](https://github.com/allenhutchison/obsidian-gemini/pull/1207)

The two vault search tools (`find_files_by_name`, `find_files_by_content`) each re-implemented the same two-clause agent-scope filter — exclude system folders, then scope to the active project via a `projectRoot + '/'` boundary check — independently in each tool, including the subtle trailing-slash invariant that prevents `projectRoot = "Foo"` from spuriously matching `Foobar/note.md`. Extracts a single `isFileInAgentScope(file, plugin, projectRoot)` predicate into `src/tools/vault/utils.ts`, used by both tools. Pure code motion, no behavior change.

### [#1206 refactor: consolidate legacy history-entry decode into a leaf helper](https://github.com/allenhutchison/obsidian-gemini/pull/1206)

The backward-compat rule that a stored history entry may carry a legacy top-level `text` or `message` field instead of canonical `parts` was re-implemented at five call sites across the Gemini and Ollama API clients and the context manager, each with its own ad-hoc cast. Consolidates the decode logic into a new leaf module, `src/utils/history-normalize.ts`, exposing separate nullish- and truthiness-precedence extractors (`getLegacyEntryText` / `getLegacyEntryTextTruthy`) since the two families of call sites actually differ in behavior at the empty-string edge — a distinction the PR calls out explicitly rather than papering over with one shared helper.

### [#1208 refactor(gemini): dedup identical streaming-response return into a builder](https://github.com/allenhutchison/obsidian-gemini/pull/1208)

An `audit-architecture` nightly sweep found `generateStreamingResponse` in the Gemini client assembling a byte-identical 6-field `ModelResponse` object twice — once on success, once in the cancelled-in-catch path. Hoists a local `buildResponse()` closure over the accumulators and routes both return sites through it. Pure code motion.

### [#1209 refactor(agent): consolidate AgentLoopResult assembly into a builder](https://github.com/allenhutchison/obsidian-gemini/pull/1209)

Companion `audit-architecture` finding in `AgentLoop.run`: every terminal path rebuilt the full 8-field `AgentLoopResult` inline, repeating the same five default status flags across five inline sites and two existing helpers. Adds a private `makeResult()` builder that defaults the flags to `false` so each call site only spells out what's true for it, and routes all seven return sites through it.
