# Daily changelog — July 15, 2026

**Date:** 2026-07-15
**PRs merged:** 3

---

## Summary

Quiet day — two small `audit-architecture` DRY refactors in the agent view, plus the previous day's daily-update housekeeping PR merging.

## What shipped

### [#1213 refactor(agent-view): extract shared buildCompactionEntry helper](https://github.com/allenhutchison/obsidian-gemini/pull/1213)

The `audit-architecture` nightly sweep flagged that two code paths — the pre-turn compaction path in `agent-view-send.ts` and the mid-loop `onMidLoopCompaction` hook in `agent-view-tools.ts` — each built a byte-identical "Context Compacted" `GeminiConversationEntry`. Both now call a new shared `buildCompactionEntry(summaryText, model)` helper in `src/ui/agent-view/compaction-notice.ts`, removing the drift risk on that persisted callout string. Pure code motion, no behavior change.

### [#1214 refactor(agent-view): unify tool→icon map into a shared leaf module](https://github.com/allenhutchison/obsidian-gemini/pull/1214)

Another `audit-architecture` DRY finding: two overlapping tool-name → Lucide-icon maps had drifted apart (`agent-view-messages.ts`'s local map covered 8 tools, `agent-view-tool-display.ts`'s `TOOL_ICONS` covered 12). Consolidated into one `TOOL_ICONS` map in a new `src/ui/agent-view/tool-icons.ts`. The one visible effect: message-history tool rows now render tool-specific icons for `google_search`, `google_maps`, `fetch_url`, and `generate_image` instead of falling back to the generic tool icon, matching what the live activity block already showed.

### [#1216 chore(daily): 2026-07-14 daily update](https://github.com/allenhutchison/obsidian-gemini/pull/1216)

The automated daily housekeeping run for 2026-07-14, bundling that day's changelog, a product-docs audit (9 drift fixes across `AGENTS.md` and several `docs/guide/` files), and a no-op triage-issues pass.
